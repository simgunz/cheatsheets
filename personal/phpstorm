Launch from shell to make ssh keys available

Disable listen for incoming connections when profiling

# Path mappings
Settings > Servers > Use path mappings

Change mapping between magentouno and magento

